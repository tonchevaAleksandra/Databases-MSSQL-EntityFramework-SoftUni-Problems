USE SoftUni

SELECT EmployeeID,
       FirstName,
       LastName,
       Salary,
       DENSE_RANK() OVER (ORDER BY Salary DESC ) AS RANK
FROM Employees
ORDER BY EmployeeID, Salary DESC
WHERE Salary BETWEEN (10000, 50000)
